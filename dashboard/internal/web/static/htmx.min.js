/* HTMX is no longer required by the main pages (raw SSE used).
   This file is kept as an empty shim to avoid 404s if any template
   references it. The production build can replace it with a real htmx. */
(function () {})();
